# SPDX-FileCopyrightText: 2026 liudng <liudng@users.noreply.github.com>
# SPDX-License-Identifier: GPL-3.0-or-later

app-id: io.github.liudng.hello
runtime: org.kde.Platform
runtime-version: '6.8'
sdk: org.kde.Sdk
command: hello
finish-args:
  - --socket=wayland
  - --share=ipc
  - --filesystem=host
  - --device=dri
cleanup:
  - /include
  - /lib/pkgconfig
  - /lib/cmake
  - '*.a'
  - '*.la'
modules:
  - name: hello
    buildsystem: cmake-ninja
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DCMAKE_INSTALL_PREFIX=/app
      - -DHELLO_BUILD_TESTS=OFF
      - -DHELLO_BUILD_FUZZ=OFF
    sources:
      - type: dir
        path: ../..
      - type: shell
        commands:
          - mkdir -p build
